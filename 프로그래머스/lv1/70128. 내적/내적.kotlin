class Solution {
    fun solution(a: IntArray, b: IntArray): Int {
        var answer: Int = 0
        
        for(index in 0 .. a.size-1){
            answer += a[index] * b[index]
        }
        return answer
    }
}