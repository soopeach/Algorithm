class Solution {
    fun solution(numbers: IntArray): Int {
        var answer: Int = 0
        
        for(no in 1.. 9) answer += no
        
        numbers.forEach{
            answer -= it
        }
        
        return answer
    }
    

}